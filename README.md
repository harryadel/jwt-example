Install node with [n](https://github.com/mklement0/n-install):

```
curl -L https://git.io/n-install | bash
n stable
```

Install jwt module:

```
npm install
```

Run the file:

```
node index.js
```
